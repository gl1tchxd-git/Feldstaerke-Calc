use crate::biot_savart::biot_savart;

pub struct Solenoid {
    pub current: f64,
    pub turns: usize,
    pub length: f64,
    pub radius: f64,
}

impl Solenoid {
    pub fn calculate() -> f64 {

    }
}